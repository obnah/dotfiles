
export TERM=xterm-256color
export PATH=$PATH:/usr/local/go/bin
export PATH="$PATH:$(go env GOPATH)/bin"

alias work='tmux new -s aaron || tmux a -t aaron'

kubectl 1>/dev/null 2>&1 && source <(kubectl completion bash)          

